---
title: Networks
sidebar_label: Networks
---

# Networks

Every value that differs between mainnet and testnet lives in this section: contract addresses, protocol parameters, caps, and wallet setup. The rest of these docs is network-neutral. **Always confirm the current values here before you transact.**

## Mainnet vs Testnet

TBV runs on two networks. Mainnet involves **real value** and is deliberately conservative; testnet is a free rehearsal environment with no real value.

| | Mainnet | Testnet |
|---|---|---|
| **Bitcoin network** | Bitcoin Mainnet | Signet |
| **Ethereum network** | Ethereum Mainnet | Sepolia |
| **Value at risk** | Real BTC and real value | None (test coins from a faucet) |
| **Access** | Allowlisted / invite (phased) | Open |

If you want to practise the full flow before committing real funds, start on testnet.

## Caps and the phased, allowlisted launch

On mainnet, TBV is a **capped, allowlisted Beta**, and those limits are a first-class part of the network — not a footnote:

- **Phased, allowlisted rollout.** Access begins gated by an allowlist / invite, enforced on-chain by `AllowListPolicy`, and opens up in phases as launch confidence grows. Each phase also carries an **operational monthly credit cap** — a launch limit managed operationally, not a smart-contract parameter.
- **On-chain exposure caps from day one.** `CapPolicy` enforces a per-application total (`totalCapBTC`) and a per-address cap (`perAddressCapBTC`); `ProtocolParams` enforces a per-vault maximum (`maxPegInAmount`) at peg-in.

For the full framing of phases, allowlisting, and what to expect, see [Beta Mainnet: what to expect](../start-here/beta-mainnet.mdx).

## Mainnet

All mainnet-specific numeric values — addresses, parameters, and caps — are **to be announced at launch**.

- [Setup (Mainnet)](./mainnet/setup.mdx) — supported BTC and ETH wallets, the mainnet networks, dual-wallet connect, and mainnet confirmation depth.
- [Ethereum smart contract addresses (Mainnet)](./mainnet/contract-addresses.mdx) — the canonical contract and token addresses. Always check here before transacting.
- [Protocol parameters & caps (Mainnet)](./mainnet/protocol-parameters-and-caps.mdx) — the full parameter and cap tables, plus how versioning and the phased launch work.

## Testnet

Open to everyone. Collateral is signet BTC and borrowed assets are mock tokens on Sepolia, so nothing carries monetary value.

- [Setup (Testnet)](./testnet/setup.mdx) — wallets, networks, faucets, and the TBV Testnet app URL.
- [Ethereum smart contract addresses (Testnet)](./testnet/contract-addresses.mdx) — the deployed Sepolia addresses.
- [Protocol parameters (Testnet)](./testnet/protocol-parameters.mdx) — the live parameter and cap values.
